<section class='containerfluid p-0 m-0'>
  <div class='row m-0'>
    <div class='col-2 left-side bg-secondary p-0 m-0'>
      <%= render 'problems/left-side' %>
    </div>
    <div class='col-10 p-0 m-0'>
      <%= flash_message %>
      <%= render 'problems/navbar' %>
      <% User.order("created_at DESC").all.each do |p| %>
        <div class="container col-4 mt-5 shadow-lg p-3 mb-5 bg-white rounded all-users">
          <%= user_avatar(p) %>
          <%=link_to p.name, user_path(id: p.id) %>
        </div>
      <% end %>
    </div>
  </div>
</section>

